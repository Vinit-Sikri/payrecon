server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — react-router owns client-side routes like /orders,
    # /ledger, etc., which don't exist as real files.
    location / {
        try_files $uri /index.html;
    }
}
